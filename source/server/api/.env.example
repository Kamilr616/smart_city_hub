PORT=4200
JWT_SECRET_KEY=replace-with-a-long-random-secret
MONGODB_URI=mongodb://127.0.0.1:27017/smart-city-hub
CORS_ORIGIN=http://localhost:5173

# Required only while running `npm run seed:admin` for a fresh database.
# Remove INITIAL_ADMIN_PASSWORD after the administrator has been created.
INITIAL_ADMIN_EMAIL=
INITIAL_ADMIN_NAME=
INITIAL_ADMIN_PASSWORD=
